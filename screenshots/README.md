# Screenshots

Add a screenshot of the desktop here named `preview.png`.

Suggested workflow:
1. Open `index.html` in browser
2. Take a screenshot (full page)
3. Save as `screenshots/preview.png`

The README.md references `screenshots/preview.png` for the hero image.
